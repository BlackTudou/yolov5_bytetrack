# 人群密集场景参数调优指南

## 当前推荐配置（已优化）

已针对人群密集且有重叠的场景优化，减少目标丢失：

```python
CONF_THRESHOLD = 0.55        # 平衡检测精度
IOU_THRESHOLD = 0.35         # 处理重叠框
HIGH_THRESH = 0.6            # 平衡筛选
MATCH_THRESH = 0.75          # 关键！降低以匹配更多目标
TRACK_BUFFER = 50             # 增加缓冲减少ID切换
```

## 参数调整原理

### 为什么之前会丢失目标？

之前参数：
- `MATCH_THRESH = 0.85` **太高**
  - 0.85意味着IoU必须达到85%才能匹配
  - 人群密集时，目标位置变化大，无法达到这么高的IoU
  - 结果：目标无法匹配，丢失跟踪

现在参数：
- `MATCH_THRESH = 0.75` **更平衡**
  - 允许更大的位置变化（75% IoU）
  - 既能跟踪到目标，又不会错误关联

### 其他关键调整

**TRACK_BUFFER: 40 → 50**
- 增加缓冲帧数，短暂遮挡后仍能恢复跟踪
- 减少ID切换频率

**CONF_THRESHOLD: 0.6 → 0.55**
- 稍微降低，确保检测到足够的目标
- 避免因为检测不足导致跟踪失败

**HIGH_THRESH: 0.65 → 0.6**
- 降低筛选标准
- 允许更多检测框参与跟踪

## 如果仍然丢失目标

### 方法1: 进一步降低匹配阈值

```python
MATCH_THRESH = 0.7   # 或 0.65
```

**注意**: 降低太多可能错误关联不同目标

### 方法2: 增加缓冲帧数

```python
TRACK_BUFFER = 60   # 或 70
```

特别适合频繁遮挡的场景

### 方法3: 降低检测阈值

```python
CONF_THRESHOLD = 0.5   # 确保不遗漏目标
```

### 方法4: 综合调整（推荐）

如果人群非常密集，频繁出现丢失：

```python
CONF_THRESHOLD = 0.5
MATCH_THRESH = 0.7
TRACK_BUFFER = 60
```

## 参数对比表

| 参数 | 原值 | 新值 | 影响 |
|------|------|------|------|
| CONF_THRESHOLD | 0.6 | **0.55** ↓ | 降低→检测更多目标 |
| IOU_THRESHOLD | 0.35 | 0.35 | 保持（适合密集） |
| HIGH_THRESH | 0.65 | **0.6** ↓ | 降低→允许更多框 |
| MATCH_THRESH | 0.85 | **0.75** ↓ | 降低→更易匹配 |
| TRACK_BUFFER | 40 | **50** ↑ | 增加→更持久跟踪 |

## 进一步调参

### 场景：仍然有很多ID切换

```python
# 降低匹配阈值，提高缓冲
MATCH_THRESH = 0.7
TRACK_BUFFER = 60
```

### 场景：出现错误关联（跟踪到错误目标）

```python
# 提高匹配阈值，降低缓冲
MATCH_THRESH = 0.8
TRACK_BUFFER = 40
HIGH_THRESH = 0.65
```

### 场景：检测不足（很多目标检测不到）

```python
# 降低所有阈值
CONF_THRESHOLD = 0.45
TRACK_THRESH = 0.45
HIGH_THRESH = 0.55
```

## 实时调试技巧

运行程序时观察：

1. **检测数量**（左上角显示的Tracks数）
   - 如果很少：降低 CONF_THRESHOLD
   - 如果太多：提高 CONF_THRESHOLD

2. **ID稳定性**（观察同一个人的ID是否保持）
   - ID经常切换：降低 MATCH_THRESH 或增加 TRACK_BUFFER
   - ID稳定：当前参数合适

3. **短暂消失后恢复**（人短暂被遮挡）
   - 能恢复：当前 TRACK_BUFFER 合适
   - 不能恢复：增加 TRACK_BUFFER

## 快速测试

运行demo后，检查以下指标：

- ✅ **跟踪持续性**: 同一人的ID保持稳定
- ✅ **覆盖度**: 大部分人都有ID
- ✅ **错误率**: ID不会频繁跳变

如果不满足，按上述方法调整参数。

## 参考配置对比

| 场景 | CONF | HIGH | MATCH | BUFFER | 特点 |
|------|------|------|-------|--------|------|
| 当前推荐 | 0.55 | 0.6 | 0.75 | 50 | 平衡 |
| 更宽松 | 0.5 | 0.6 | 0.7 | 60 | 避免丢失 |
| 更严格 | 0.6 | 0.65 | 0.8 | 40 | 更精确 |
| 极度密集 | 0.5 | 0.55 | 0.65 | 70 | 最大宽容度 |

## 操作建议

1. **先用当前默认参数**（已经优化过）
2. **观察效果**，看是否还有丢失
3. **根据问题选择对应方法调整**
4. **渐进式调整**，每次只改一个参数观察效果

