# 快速参数调优说明

## 如何开始

直接运行demo程序：
```bash
python demo_detect_and_track.py
```

## 如何调参

### 方法1: 修改文件顶部参数（推荐）

打开 `demo_detect_and_track.py`，修改第13-29行的参数：

```python
# YOLOv5 检测参数
CONF_THRESHOLD = 0.5        # 改为 0.6 会更严格
IOU_THRESHOLD = 0.45

# ByteTrack 跟踪参数
TRACK_THRESH = 0.5
HIGH_THRESH = 0.6
MATCH_THRESH = 0.8          # 改为 0.85 跟踪更稳定
TRACK_BUFFER = 30
```

### 方法2: 使用场景预设

取消注释对应的场景配置（第32-50行）：

```python
# 场景1: 人群密集环境
CONF_THRESHOLD, IOU_THRESHOLD = 0.6, 0.35
HIGH_THRESH, MATCH_THRESH, TRACK_BUFFER = 0.65, 0.85, 40
```

## 常见问题与解决方案

### 问题1: 检测到很多误报（不是人）

**解决**: 提高 `CONF_THRESHOLD`
```python
CONF_THRESHOLD = 0.6  # 或 0.7
```

### 问题2: 漏检很多人

**解决**: 降低 `CONF_THRESHOLD`
```python
CONF_THRESHOLD = 0.4  # 或 0.3
```

### 问题3: 多人聚集时检测框重叠混乱

**解决**: 降低 `IOU_THRESHOLD`
```python
IOU_THRESHOLD = 0.35  # 或 0.3
```

### 问题4: 同一人的ID频繁切换

**解决**: 提高 `MATCH_THRESH`
```python
MATCH_THRESH = 0.85  # 或 0.9
```

### 问题5: 人短暂消失后ID换了

**解决**: 增加 `TRACK_BUFFER`
```python
TRACK_BUFFER = 50  # 或 70
```

### 问题6: 跟踪到错误的目标

**解决**: 同时调整两个参数
```python
HIGH_THRESH = 0.65      # 提高
MATCH_THRESH = 0.85     # 提高
```

## 参数速查表

| 参数 | 增大影响 | 减小影响 | 常用范围 |
|------|---------|---------|---------|
| `CONF_THRESHOLD` | 更严格，减少误报 | 更宽松，可能漏检 | 0.4-0.7 |
| `IOU_THRESHOLD` | 更稀疏，减少重叠 | 更密集，允许密集 | 0.3-0.5 |
| `TRACK_THRESH` | 更严格跟踪 | 更宽松跟踪 | 0.4-0.6 |
| `HIGH_THRESH` | 更严格筛选 | 更宽松筛选 | 0.55-0.7 |
| `MATCH_THRESH` | 更稳定ID | 更灵活ID | 0.6-0.9 |
| `TRACK_BUFFER` | 更持久跟踪 | 更快清理 | 20-70 |

## 调参流程

1. **第一步**: 调整 `CONF_THRESHOLD`，确保能正确检测到人
2. **第二步**: 调整 `IOU_THRESHOLD`，解决密集人群问题
3. **第三步**: 调整 `MATCH_THRESH`，稳定跟踪ID
4. **第四步**: 调整 `TRACK_BUFFER`，处理短暂消失
5. **微调**: 根据效果微调其他参数

## 实时查看效果

运行程序后会显示：
- **左上角**: 当前帧号、跟踪数量、跟踪ID列表、FPS
- **检测框**: 每个目标用不同颜色标注，显示ID和置信度

按键：
- `q`: 退出
- `s`: 暂停/继续

## 性能优化

### 如果你有GPU
```python
DEVICE = 'cuda'
IMG_SIZE = 800  # 可以用更大的尺寸
```

### 如果只有CPU
```python
DEVICE = 'cpu'
IMG_SIZE = 416  # 用更小的尺寸
CONF_THRESHOLD = 0.6  # 稍微提高以减少计算量
```

## 详细说明

查看 `调参指南.md` 获取：
- 每个参数的详细说明
- 不同场景的推荐配置
- 高级调参技巧

