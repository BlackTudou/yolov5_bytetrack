# 数据流详细分析

## 1. 整体数据流概览

```
视频帧 (numpy array, BGR)
    ↓
[YOLOv5s检测] → 检测结果列表
    ↓
[ByteTrack跟踪] → 跟踪对象列表
    ↓
[距离计算] → 违规信息
    ↓
[可视化] → 标注图像
```

## 2. YOLOv5s 数据流

### 2.1 输入格式

**输入类型**：`numpy.ndarray` (BGR图像)

**输入形状**：
```python
# 原始图像
img_bgr.shape = (height, width, 3)  # 例如: (1080, 1920, 3)

# 预处理后
img_tensor.shape = (1, 3, 640, 640)  # batch_size=1, channels=3, size=640x640
```

**输入数据示例**：
```python
img_bgr = np.array([
    [[B, G, R], [B, G, R], ...],  # 第一行像素
    [[B, G, R], [B, G, R], ...],  # 第二行像素
    ...
], dtype=np.uint8)  # 值范围: 0-255
```

### 2.2 预处理流程

```python
# 步骤1: BGR → RGB
img_rgb = cv2.cvtColor(img_bgr, cv2.COLOR_BGR2RGB)
# 形状: (H, W, 3), dtype=uint8

# 步骤2: Resize到640x640
img_resized = cv2.resize(img_rgb, (640, 640))
# 形状: (640, 640, 3), dtype=uint8

# 步骤3: 归一化到[0, 1]
img_normalized = img_resized.astype(np.float32) / 255.0
# 形状: (640, 640, 3), dtype=float32, 值范围: 0.0-1.0

# 步骤4: 转换为Tensor并调整维度
img_tensor = torch.from_numpy(img_normalized).permute(2, 0, 1)
# 形状: (3, 640, 640), dtype=torch.float32

# 步骤5: 添加batch维度
img_tensor = img_tensor.unsqueeze(0)
# 形状: (1, 3, 640, 640), dtype=torch.float32

# 步骤6: 移动到设备
img_tensor = img_tensor.to(device)  # 'cpu' 或 'cuda'
```

### 2.3 模型推理

**模型输入**：
```python
# YOLOv5s模型期望输入
input_tensor: torch.Tensor
    shape: (1, 3, 640, 640)
    dtype: torch.float32
    range: [0.0, 1.0]
    device: cpu/cuda
```

**模型输出（原始）**：
```python
# YOLOv5原始输出
results: torch.Tensor 或 Results对象
    # 如果是tensor:
    shape: (1, num_detections, 6)
    # 6 = [x1, y1, x2, y2, confidence, class_id]

    # 如果是Results对象（YOLOv5 v5+）:
    results.pandas().xyxy[0]  # pandas DataFrame
    columns: ['xmin', 'ymin', 'xmax', 'ymax', 'confidence', 'class', 'name']
```

### 2.4 YOLOv5s 输出格式

**输出类型**：`List[List[float, float, float, float, float, int]]`

**每个检测结果**：
```python
detection = [
    x1,          # float: 边界框左上角x坐标（像素）
    y1,          # float: 边界框左上角y坐标（像素）
    x2,          # float: 边界框右下角x坐标（像素）
    y2,          # float: 边界框右下角y坐标（像素）
    confidence,  # float: 置信度 (0.0-1.0)
    class_id     # int: 类别ID (0=person, 1=bicycle, ...)
]
```

**完整输出示例**：
```python
detections = [
    [100.5, 200.3, 350.2, 650.1, 0.89, 0],  # 第1个人
    [500.1, 150.7, 720.4, 580.9, 0.76, 0],  # 第2个人
    [800.2, 300.5, 950.8, 700.3, 0.65, 0],  # 第3个人
]
```

### 2.5 后处理转换

**转换为tlwh格式**（用于ByteTrack）：
```python
# 从 [x1, y1, x2, y2] 转换为 [x, y, w, h]
for detection in detections:
    x1, y1, x2, y2 = detection[:4]

    tlwh = [
        x1,              # x: 左上角x坐标
        y1,              # y: 左上角y坐标
        x2 - x1,         # w: 宽度
        y2 - y1          # h: 高度
    ]

    # 最终输出格式
    output = [tlwh, confidence, class_id]
    # 例如: [[100.5, 200.3, 249.7, 449.8], 0.89, 0]
```

## 3. ByteTrack 数据流

### 3.1 输入格式

**输入类型**：`List[List]` - 检测结果列表

**输入格式**：
```python
detections = [
    [tlwh, score, cls_id],  # 检测1
    [tlwh, score, cls_id],  # 检测2
    ...
]

# 其中:
tlwh = [x, y, w, h]  # top-left width height
    x: float  # 左上角x坐标
    y: float  # 左上角y坐标
    w: float  # 宽度
    h: float  # 高度

score: float  # 置信度 (0.0-1.0)
cls_id: int   # 类别ID (0=person)
```

**输入示例**：
```python
detections = [
    [[100.5, 200.3, 249.7, 449.8], 0.89, 0],  # 检测到1个人
    [[500.1, 150.7, 220.3, 430.2], 0.76, 0],  # 检测到1个人
    [[800.2, 300.5, 150.6, 399.8], 0.65, 0],  # 检测到1个人
]
```

### 3.2 ByteTrack 内部处理

**步骤1：转换为STrack对象**
```python
# 每个检测转换为STrack对象
stracks = []
for tlwh, score, cls_id in detections:
    strack = STrack(tlwh, score, cls_id)
    # 创建时:
    # - 初始化卡尔曼滤波状态
    # - 设置边界框
    # - track_id = 0 (待分配)
    # - state = 'New'
    stracks.append(strack)
```

**步骤2：分离置信度**
```python
# 根据置信度阈值分离
high_conf_detections = [d for d in stracks if d.score >= 0.6]
low_conf_detections = [d for d in stracks if 0.5 <= d.score < 0.6]
```

**步骤3：关联已跟踪轨迹**
```python
# 计算IoU距离矩阵
iou_matrix = compute_iou_distance(tracked_tracks, high_conf_detections)
# 形状: (num_tracks, num_detections)

# 匈牙利算法匹配
matched, unmatched_tracks, unmatched_dets = linear_assignment(iou_matrix)
# matched: [(track_idx, det_idx), ...]
# unmatched_tracks: [track_idx, ...]
# unmatched_dets: [det_idx, ...]
```

**步骤4：更新匹配的轨迹**
```python
for track_idx, det_idx in matched:
    track = tracked_tracks[track_idx]
    det = high_conf_detections[det_idx]

    # 更新轨迹
    track.update(det, frame_id)
    # - 更新边界框
    # - 更新置信度
    # - 更新卡尔曼滤波状态
    # - track_len += 1
```

**步骤5：重新激活丢失轨迹**
```python
# 关联丢失轨迹与高置信度检测
reactivated, unmatched_lost = associate_detections_to_trackers(
    lost_tracks, high_conf_detections)

for track_idx, det_idx in reactivated:
    track = lost_tracks[track_idx]
    det = high_conf_detections[det_idx]

    # 重新激活
    track.re_activate(det, frame_id, new_id=False)
    # - 恢复轨迹状态
    # - 更新边界框
    # - state = 'Tracked'
```

**步骤6：创建新轨迹**
```python
# 未匹配的高置信度检测创建新轨迹
for det in unmatched_high_confs:
    det.activate(frame_id, new_track_id)
    # - 分配新的track_id
    # - state = 'Tracked'
    # - is_activated = True
    tracked_stracks.append(det)
```

**步骤7：处理低置信度检测**
```python
# 尝试关联未匹配的轨迹与低置信度检测
if low_conf_detections:
    matched_low, _, _ = associate_detections_to_trackers(
        unmatched_tracks, low_conf_detections)

    for track_idx, det_idx in matched_low:
        track.re_activate(low_conf_detections[det_idx], frame_id)
```

### 3.3 ByteTrack 输出格式

**输出类型**：`List[STrack]` - 跟踪对象列表

**STrack对象属性**：
```python
class STrack:
    tlwh: np.ndarray      # [x, y, w, h] 边界框
    track_id: int         # 唯一跟踪ID (1, 2, 3, ...)
    score: float          # 置信度
    state: str            # 'Tracked'/'Lost'/'Removed'
    frame_id: int         # 当前帧ID
    track_len: int        # 轨迹长度（存在帧数）
    is_activated: bool    # 是否激活
    mean: np.ndarray      # 卡尔曼滤波状态 [cx, cy, w, h]
```

**输出示例**：
```python
tracked_objects = [
    STrack(
        tlwh=[100.5, 200.3, 249.7, 449.8],
        track_id=1,
        score=0.89,
        state='Tracked',
        frame_id=100,
        track_len=50
    ),
    STrack(
        tlwh=[500.1, 150.7, 220.3, 430.2],
        track_id=2,
        score=0.76,
        state='Tracked',
        frame_id=100,
        track_len=45
    ),
    ...
]
```

## 4. 距离计算数据流

### 4.1 输入格式

**输入类型**：两个 `STrack` 对象

```python
person1: STrack(
    tlwh=[x1, y1, w1, h1],
    track_id=1,
    ...
)

person2: STrack(
    tlwh=[x2, y2, w2, h2],
    track_id=2,
    ...
)
```

### 4.2 计算流程

**步骤1：计算像素距离**
```python
# 获取边界框
box1 = person1.tlwh  # [x1, y1, w1, h1]
box2 = person2.tlwh  # [x2, y2, w2, h2]

# 计算底部中心点（脚部位置）
center1 = [
    box1[0] + box1[2] / 2,  # x中心
    box1[1] + box1[3]       # y底部（脚部）
]
center2 = [
    box2[0] + box2[2] / 2,
    box2[1] + box2[3]
]

# 欧几里得距离
pixel_distance = sqrt((center1[0] - center2[0])² + (center1[1] - center2[1])²)
```

**步骤2：估算深度距离**
```python
# 估算person1与相机的距离
dist1 = calibrator.estimate_distance(box1[2], box1[3])
# 输入: width, height (像素)
# 输出: 距离（米）

# 估算person2与相机的距离
dist2 = calibrator.estimate_distance(box2[2], box2[3])

# 平均深度
avg_depth = (dist1 + dist2) / 2
```

**步骤3：转换为真实距离**
```python
# 像素距离 → 真实距离
real_distance = calibrator.pixel_to_real_distance(
    pixel_distance,
    avg_depth
)
```

### 4.3 输出格式

**输出类型**：`Tuple[float, float]`

```python
return (pixel_distance, real_distance)
# pixel_distance: float  # 像素距离
# real_distance: float    # 真实距离（米）

# 示例:
return (156.3, 1.8)  # 像素距离156.3，真实距离1.8米
```

## 5. 完整数据流示例

### 示例：检测3个人

```python
# === 输入：视频帧 ===
frame = np.array([...], shape=(1080, 1920, 3), dtype=uint8)

# === YOLOv5s检测 ===
detections = [
    [[100.5, 200.3, 249.7, 449.8], 0.89, 0],  # 人1
    [[500.1, 150.7, 220.3, 430.2], 0.76, 0],  # 人2
    [[800.2, 300.5, 150.6, 399.8], 0.65, 0],  # 人3
]

# === ByteTrack跟踪 ===
tracked_objects = [
    STrack(tlwh=[100.5, 200.3, 249.7, 449.8], track_id=1, ...),  # 人1，ID=1
    STrack(tlwh=[500.1, 150.7, 220.3, 430.2], track_id=2, ...),  # 人2，ID=2
    STrack(tlwh=[800.2, 300.5, 150.6, 399.8], track_id=3, ...), # 人3，ID=3
]

# === 距离计算 ===
# 计算人1和人2的距离
pixel_dist_12, real_dist_12 = calculate_distance(
    tracked_objects[0],  # person1
    tracked_objects[1]    # person2
)
# 结果: (156.3, 1.8)  # 像素距离156.3，真实距离1.8米

# 计算人1和人3的距离
pixel_dist_13, real_dist_13 = calculate_distance(
    tracked_objects[0],  # person1
    tracked_objects[2]   # person3
)
# 结果: (245.7, 2.5)

# 计算人2和人3的距离
pixel_dist_23, real_dist_23 = calculate_distance(
    tracked_objects[1],  # person2
    tracked_objects[2]    # person3
)
# 结果: (189.2, 2.1)

# === 违规检测 ===
violations = []
if real_dist_12 < 1.5:  # 假设阈值1.5米
    violations.append((1, 2, 1.8))  # ID1和ID2距离1.8米，小于1.5米阈值

# === 输出：可视化结果 ===
# 绘制3个边界框（绿色）
# 绘制ID标签（ID:1, ID:2, ID:3）
# 如果violations不为空，绘制红色连线
```

## 6. 数据流图

```
┌─────────────────────────────────────────────────────────────┐
│                    视频帧 (BGR)                             │
│              shape: (H, W, 3), dtype=uint8                  │
└────────────────────┬──────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  YOLOv5s 检测                               │
│  输入: BGR图像                                               │
│  输出: detections = [[tlwh, score, class_id], ...]          │
│                                                             │
│  示例:                                                      │
│    [[[100, 200, 250, 450], 0.89, 0],                      │
│     [[500, 150, 220, 430], 0.76, 0]]                       │
└────────────────────┬──────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  ByteTrack 跟踪                             │
│  输入: detections (检测列表)                                 │
│  输出: tracked_objects (STrack对象列表)                     │
│                                                             │
│  示例:                                                      │
│    [STrack(track_id=1, tlwh=[...], state='Tracked'),       │
│     STrack(track_id=2, tlwh=[...], state='Tracked')]       │
└────────────────────┬──────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  距离计算                                    │
│  输入: 两个STrack对象                                        │
│  输出: (pixel_dist, real_dist)                              │
│                                                             │
│  示例: (156.3, 1.8)  # 像素156.3，真实1.8米                 │
└────────────────────┬──────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  违规检测                                    │
│  输入: tracked_objects列表                                  │
│  输出: violations = [(id1, id2, distance), ...]            │
│                                                             │
│  示例: [(1, 2, 1.8)]  # ID1和ID2距离1.8米                  │
└────────────────────┬──────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  可视化输出                                  │
│  输出: 标注后的图像 (BGR)                                    │
│        - 绿色边界框                                          │
│        - ID标签                                              │
│        - 红色违规连线                                        │
│        - 距离标注                                            │
└─────────────────────────────────────────────────────────────┘
```

## 7. 关键数据结构总结

| 模块 | 输入类型 | 输入格式 | 输出类型 | 输出格式 |
|------|---------|---------|---------|---------|
| **YOLOv5s** | numpy.ndarray | (H, W, 3) BGR图像 | List[List] | `[[tlwh, score, cls_id], ...]` |
| **ByteTrack** | List[List] | `[[tlwh, score, cls_id], ...]` | List[STrack] | `[STrack(...), STrack(...)]` |
| **距离计算** | 两个STrack | `STrack(tlwh, track_id, ...)` | Tuple[float, float] | `(pixel_dist, real_dist)` |
| **违规检测** | List[STrack] | `[STrack(...), ...]` | List[Tuple] | `[(id1, id2, dist), ...]` |

## 8. 数据转换示例代码

```python
# === YOLOv5输出 → ByteTrack输入 ===
yolo_output = [[100, 200, 350, 650, 0.89, 0], ...]  # [x1,y1,x2,y2,conf,cls]

# 转换为tlwh格式
bytetrack_input = []
for x1, y1, x2, y2, conf, cls in yolo_output:
    tlwh = [x1, y1, x2-x1, y2-y1]  # [x, y, w, h]
    bytetrack_input.append([tlwh, conf, cls])

# === ByteTrack输出 → 距离计算输入 ===
bytetrack_output = [STrack(track_id=1, tlwh=[...]), ...]

# 直接使用STrack对象
person1 = bytetrack_output[0]
person2 = bytetrack_output[1]

# === 距离计算输出 → 违规检测输入 ===
pixel_dist, real_dist = calculate_distance(person1, person2)

if real_dist < threshold:
    violation = (person1.track_id, person2.track_id, real_dist)
```

