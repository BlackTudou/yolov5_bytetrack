# 目标检测与跟踪参数调优指南

## 1. YOLOv5 检测参数

### conf_threshold (置信度阈值)
- **默认值**: 0.5
- **作用**: 过滤低置信度的检测框
- **调整建议**:
  - 检测到太多误报（不是人）: 提高到 0.6-0.8
  - 漏检很多目标: 降低到 0.3-0.4
  - 室内场景建议 0.5-0.6
  - 室外复杂场景建议 0.4-0.5

```python
detections = detector.detect(frame, conf_threshold=0.5)  # 调整这里
```

### iou_threshold (NMS IoU阈值)
- **默认值**: 0.45
- **作用**: 抑制重叠检测框
- **调整建议**:
  - 目标密集场景（很多人聚集）: 降低到 0.3-0.4
  - 目标稀疏场景: 提高到 0.5-0.6
  - 目标遮挡严重: 降低到 0.3

```python
detections = detector.detect(frame, iou_threshold=0.45)  # 调整这里
```

### img_size (输入图像尺寸)
- **默认值**: 640
- **调整建议**:
  - GPU性能好: 提高到 800-1280（更准确）
  - CPU运行: 降低到 416（更快）
  - 平衡点: 640
  - 小目标多: 提高到 800-1024

## 2. ByteTrack 跟踪参数

### track_thresh (低置信度阈值)
- **默认值**: 0.5
- **作用**: 用于关联的检测框的最低置信度
- **调整建议**:
  - 检测不稳定，经常出现新ID: 降低到 0.4-0.45
  - 太多误跟踪: 提高到 0.55-0.6
  - 快节奏场景: 降低到 0.4

```python
tracker = ByteTracker(
    track_thresh=0.5,  # 调整这里
    high_thresh=0.6,
    match_thresh=0.8,
    track_buffer=30
)
```

### high_thresh (高置信度阈值)
- **默认值**: 0.6
- **作用**: 区分高/低置信度检测
- **调整建议**:
  - 跟踪ID频繁切换: 降低到 0.55-0.58
  - 跟踪多余目标: 提高到 0.65-0.7
  - 快速移动场景: 降低到 0.55

```python
tracker = ByteTracker(
    track_thresh=0.5,
    high_thresh=0.6,  # 调整这里
    match_thresh=0.8,
    track_buffer=30
)
```

### match_thresh (匹配阈值)
- **默认值**: 0.8
- **作用**: IoU匹配的阈值
- **调整建议**:
  - ID频繁切换: 降低到 0.6-0.7
  - 多人擦肩而过时错误关联: 提高到 0.85-0.9
  - 快速运动: 降低到 0.6-0.7
  - 慢速或静态: 提高到 0.8-0.85

```python
tracker = ByteTracker(
    track_thresh=0.5,
    high_thresh=0.6,
    match_thresh=0.8,  # 调整这里
    track_buffer=30
)
```

### track_buffer (跟踪缓冲)
- **默认值**: 30
- **作用**: 丢失目标后保留的帧数
- **调整建议**:
  - 短暂遮挡频繁出现: 提高到 50-70
  - 内存和性能受限: 降低到 15-20
  - 高帧率视频: 按比例增加
  - 低帧率视频: 降低

```python
tracker = ByteTracker(
    track_thresh=0.5,
    high_thresh=0.6,
    match_thresh=0.8,
    track_buffer=30  # 调整这里
)
```

### frame_rate (视频帧率)
- **默认值**: 30
- **作用**: 用于计算track_buffer
- **调整建议**: 设置为实际视频帧率
  - 高清视频 30fps: 30
  - 高清视频 25fps: 25
  - 慢动作 60fps: 60

```python
tracker = ByteTracker(
    frame_rate=30,  # 调整这里（视频实际帧率）
    track_thresh=0.5,
    ...
)
```

## 3. 常见场景的推荐配置

### 场景1: 人群密集（候车厅、商场）
```python
# 检测参数
conf_threshold = 0.6          # 提高，减少误检
iou_threshold = 0.35          # 降低，允许更密集

# 跟踪参数
track_thresh = 0.5
high_thresh = 0.65           # 提高，只要高置信度
match_thresh = 0.85          # 提高，避免错误关联
track_buffer = 40            # 增加，短暂遮挡很多
```

### 场景2: 空旷环境（操场、公园）
```python
# 检测参数
conf_threshold = 0.45        # 降低，不遗漏远距离目标
iou_threshold = 0.5          # 提高，目标相距较远

# 跟踪参数
track_thresh = 0.45          # 降低，宽松一点
high_thresh = 0.55
match_thresh = 0.75          # 降低，匹配更宽松
track_buffer = 30
```

### 场景3: 快速运动（体育场、跑道）
```python
# 检测参数
conf_threshold = 0.5
iou_threshold = 0.4

# 跟踪参数
track_thresh = 0.45          # 降低
high_thresh = 0.55           # 降低
match_thresh = 0.65          # 显著降低，允许更大变化
track_buffer = 50            # 增加，缓冲时间更长
```

### 场景4: 室内监控（走廊、大厅）
```python
# 检测参数
conf_threshold = 0.55        # 提高，室内光线稳定
iou_threshold = 0.45

# 跟踪参数
track_thresh = 0.5
high_thresh = 0.6
match_thresh = 0.85          # 提高，轨迹稳定
track_buffer = 30
```

### 场景5: 低光照或夜视
```python
# 检测参数
conf_threshold = 0.4         # 显著降低
iou_threshold = 0.35

# 跟踪参数
track_thresh = 0.4           # 降低
high_thresh = 0.5            # 降低
match_thresh = 0.7           # 降低
track_buffer = 60            # 大幅增加
```

## 4. 调试技巧

### 步骤1: 先调检测准确度
```python
# 在demo中临时添加打印
print(f"检测到 {len(detections)} 个目标")
for det in detections:
    print(f"  置信度: {det[1]:.2f}, 坐标: {det[0]}")
```

### 步骤2: 再调跟踪稳定性
```python
# 观察同一人在不同帧的ID是否一致
print(f"当前跟踪ID: {[obj.track_id for obj in tracked_objects]}")
```

### 步骤3: 对比调整前后
- 参数A: 记录ID切换次数
- 参数B: 记录ID切换次数
- 选择切换次数更少的配置

## 5. 性能优化

### CPU模式
```python
# 降低检测精度，提升速度
img_size = 416
conf_threshold = 0.5
track_thresh = 0.5
```

### GPU模式
```python
# 提升检测精度
img_size = 800
conf_threshold = 0.5
track_thresh = 0.5
```

## 6. 监控指标

运行程序时观察：
1. **检测准确率**: 是否正确检测到所有人
2. **ID稳定性**: 同一人的ID是否保持
3. **误跟踪率**: 是否跟随到错误目标
4. **漏跟踪率**: 是否频繁丢失目标
5. **处理速度**: 能否达到实时处理

## 7. 快速调参流程

1. **看检测结果**: 调 `conf_threshold` 和 `iou_threshold`
2. **看ID稳定性**: 调 `match_thresh`
3. **看跟踪持续性**: 调 `track_buffer`
4. **平衡准确度和性能**: 调 `img_size`

## 8. 实际操作

在 `demo_detect_and_track.py` 顶部修改参数：

```python
# 针对您的场景修改这些值
CONF_THRESHOLD = 0.5      # 根据检测准确度调整
IOU_THRESHOLD = 0.45      # 根据密集程度调整
TRACK_THRESH = 0.5        # 根据跟踪稳定性调整
HIGH_THRESH = 0.6         # 根据ID切换频率调整
MATCH_THRESH = 0.8        # 根据关联准确度调整
TRACK_BUFFER = 30         # 根据遮挡情况调整
```

