# 快速运行指南

## 方法1：直接运行主程序（推荐）

### 使用视频文件
```bash
python main.py --source video --video palace.mp4 --device cpu
```

### 使用摄像头
```bash
python main.py --source camera --device cpu
```

## 方法2：运行示例文件

### 1. 编辑 example_usage.py
打开 `example_usage.py`，取消注释你想运行的示例：

```python
if __name__ == '__main__':
    # 取消注释其中一个：
    example_basic()           # 示例1：基础用法
    # example_custom()        # 示例2：自定义配置
    # example_calibration()   # 示例4：标定测试
    # example_violation_detection()  # 示例5：违规检测测试
```

### 2. 运行示例
```bash
python example_usage.py
```

## 各示例说明

### 示例1：基础用法 (example_basic)
- 使用默认摄像头
- 默认参数
- 适合快速测试

### 示例2：自定义配置 (example_custom)
- 自定义置信度和距离
- 使用视频文件
- 演示参数调优

### 示例3：单图处理 (example_image_processing)
- 处理单张图片
- 演示检测和跟踪API

### 示例4：标定测试 (example_calibration)
- 测试距离估计算法
- 保存和加载标定参数

### 示例5：违规检测 (example_violation_detection)
- 模拟两个人员
- 计算距离
- 检测是否违规

## 快速开始

### 最简单的方式
```bash
# 直接运行主程序测试视频
python main.py --source video --video palace.mp4 --device cpu
```

### 如果想测试摄像头
```bash
# 确保摄像头连接
python test_camera.py  # 先测试摄像头
python main.py --source camera --device cpu
```

## 命令行参数说明

```bash
python main.py [选项]
```

### 常用参数：
- `--source`: 视频源 (`camera` 或 `video`)
- `--video`: 视频文件路径（当 source=video 时）
- `--conf`: 置信度阈值 (默认 0.25)
- `--min-distance`: 最小安全距离，单位米 (默认 1.5)
- `--device`: 运行设备 (`cpu` 或 `cuda`)
- `--img-size`: 输入图像尺寸 (默认 1280)

### 示例命令
```bash
# 标准模式
python main.py --source video --video palace.mp4 --device cpu

# 高精度模式
python main.py --source video --video palace.mp4 --device cpu --conf 0.2 --img-size 1280

# 使用摄像头，自定义距离
python main.py --source camera --min-distance 2.0 --device cpu

# GPU加速
python main.py --source video --video palace.mp4 --device cuda
```

## 按Q键退出
运行后，在视频窗口按 'q' 键可以退出程序。

